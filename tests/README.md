# Tests

Foundation-level smoke tests only (constants, config shape). Feature
modules will bring their own test suites alongside their implementation
in later milestones.

Run with:

```bash
pnpm test
```
