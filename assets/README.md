# Assets

Place application icons and static build resources here:

- `icon.ico` — Windows icon (used by electron-builder)
- `icon.icns` — macOS icon (used by electron-builder)
- `icon.png` — Linux icon (used by electron-builder)

No binary assets are included in Milestone 1 — add them before running
`pnpm package`.
