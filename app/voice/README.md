# Voice Module

**Status:** Placeholder — not implemented in Milestone 1 (Project Foundation).

This directory is reserved for the voice subsystem of JARVIS. It exists now
purely so the project structure is stable and future milestones can be
added without restructuring the codebase.

**Standard placeholder contents (every reserved module follows this):**
- `README.md` — this file
- `index.ts` — empty module entry point (`export {}`)
- `types.ts` — reserved for future public type contracts (`export {}`)

Do not add implementation code, business logic, or AI logic here until its
dedicated milestone begins and is explicitly scoped.
