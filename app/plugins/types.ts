/**
 * Plugins module — reserved type contracts (placeholder).
 *
 * No interfaces are defined yet because no behavior for this module has
 * been approved. When plugins's milestone begins, its public contracts
 * should be declared here first, then implemented in index.ts and
 * supporting files — keeping the "shape" of the module reviewable before
 * any logic is written.
 */
export {};
